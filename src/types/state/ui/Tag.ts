interface Tags {
  readonly error: object;
  readonly errorCode: string;
  readonly isFetchTagsFailed: boolean;
  readonly isLoadingFetchTags: boolean;
  readonly isLoadingUploadTags: boolean;
  readonly isUploadTagsFailed: boolean;
}

export default Tags;
